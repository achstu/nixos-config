# nixos-config

This repo contains my dotfiles. It should be private, so don't clone.

### Usage

Files in `nixos` folder should be placed in `/etc/nixos/`.

Files in `config` folder should be placed in `~/.config/`.

### Sync

Just run `bash sync.sh`.

